dist: bionic

services: mongodb

before_script:
  - cp .env.example .env
  - sleep 15
  - mongo mydb_test --eval 'db.createUser({user:"travis",pwd:"test",roles:["readWrite"]});'

language: go

go:
  - 1.15
  - master

script:
  - go build
  - go test -cover