dist: bionic

services: mongodb

before_script:
  - cp .env.example .env
  - mongo mydb_test --eval 'db.createUser({user:"travis",pwd:"test",roles:["readWrite"]});'
  - sleep 15

language: go

go:
  - 1.15

script:
  - go build
  - go test -cover